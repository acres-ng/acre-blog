@AGENTS.md

## Scripts

- `dev` — start local dev server with HMR at http://localhost:3000
- `build` — compile and bundle for production (runs type-check + lint)
- `start` — serve the production build locally (run `build` first)
- `lint` — run ESLint across the project
